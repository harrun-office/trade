/**
 * Authentication Types
 *
 * This file contains all TypeScript type definitions, interfaces, and enums
 * related to authentication functionality. It provides type safety across
 * the authentication module.
 *
 * Responsibilities:
 * - Define interfaces for authentication requests/responses
 * - Define user authentication data structures
 * - Define JWT token payload structures
 * - Define authentication-related enums (user roles, auth status, etc.)
 * - Define error types specific to authentication
 * - Provide type definitions for external authentication providers
 */
